# JobTracer PRD 复审报告（分析师视角 v2）

> **评审时间：** 2026-06-02  
> **评审人：** 分析师 Agent  
> **评审文件：** `/Users/jinqianfei/openclaw-workspaces/product-solution/JobTracer PRD.md`  
> **配套文档：** `/Users/jinqianfei/openclaw-workspaces/product-solution/JobTracer智能求职工作流方案.md`

---

## 一、上次评审问题修复验证

### ✅ 问题1：Step 5-8 描述深度不足 → **已修复**

**现状：**
- Step 5 新增了完整的「意图分类表」（14种意图类型），并明确区分「自动发送」与「确认后发送」两类处理方式
- Step 6-8 均包含：触发条件、执行内容、输出物、用户交互确认点、异常处理的完整结构
- Step 7 的「模拟面试模式」有明确的执行逻辑描述（随机出题→用户回答→反馈）
- Step 8 包含数据沉淀机制（更新 ProjectTrace 索引 + 用户偏好）

**评估：** 描述深度问题已解决。

---

### ✅ 问题2：BOSS直聘风险 → **已降级处理**

**现状：**
- Step 4 明确区分 MVP 方案（`opencli boss greet`）与 Phase 2 方案（Playwright 用户登录态）
- Step 4 异常处理明确：「BOSS 发招呼被限制 → 降级为『生成招呼语 + 手动复制』模式」
- 风险等级标注为「高」，应对策略已在文档中体现

**评估：** 风险已合理降级，文档描述清晰。

---

### ✅ 问题3：数据路径不统一 → **已统一到 PRD，方案文档存在偏差**

**PRD（v1.0.0）规定路径：**
```
~/.jobtracer/
├── footprint/projects/        ← 数字足迹项目文件夹
├── memory/state.json
├── memory/resume.json
├── jobs/jd_cache/
└── ...
```

**方案文档（v1.0.0）仍使用旧路径：**
```
~/ProjectTrace/projects/        ← 旧路径，与 PRD 不一致
~/.jobtracer/                   ← 部分数据使用新路径
```

**问题：** 方案文档中的路径与 PRD 不统一，代码实现时若参考方案文档会导致路径混乱。

**建议：** 以 PRD `~/.jobtracer/` 为准，方案文档需要同步更新。

---

### ✅ 问题4：Step 5 确认逻辑不清晰 → **已修复**

**现状：**
Step 5 的「确认点设计」已清晰，核心逻辑如下：

| 消息类型 | 处理方式 | 确认？ |
|---------|---------|--------|
| 初次联系/availability/联系方式/项目经历/技术/城市 | 自动生成 + **自动发送** | ❌ |
| 约面试 | 生成 + **确认时间** | ✅ |
| 谈薪资 | 生成 + **确认数字** | ✅ |
| 谈Offer | 生成 + **确认条款** | ✅ |
| 发Offer / 拒绝接受Offer | 生成 + **确认决定** | ✅ |

**评估：** 确认逻辑已按意图分级（共5类需确认），清晰可执行。

---

## 二、Step 1-8 功能描述完整性审查

### 2.1 各 Step 触发条件 → 清晰度评估

| Step | 触发条件 | 评估 |
|------|---------|------|
| Step 1 | "开始求职" / "帮我找工作" | ✅ 清晰 |
| Step 2 | Step 1 完成 或 "生成简历" | ✅ 清晰 |
| Step 3 | Step 2 完成 / 简历确认 / 每日定时 | ✅ 清晰 |
| Step 4 | 用户确认 JD 列表 | ✅ 清晰 |
| Step 5 | 收到 HR 回复 或 主动跟进 | ✅ 清晰 |
| Step 6 | HR 明确要求发送联系方式/简历 | ✅ 清晰 |
| Step 7 | 约到面试后，说「准备面试」 | ✅ 清晰 |
| Step 8 | 拿到 offer / 求职告一段落 / 每周五 | ✅ 清晰 |

### 2.2 各 Step 执行内容 → 清晰度评估

| Step | 执行内容覆盖 | 评估 |
|------|------------|------|
| Step 1 | 8类数据源扫描 → 聚类 → 特征提取 → 输出 | ✅ 完整 |
| Step 2 | 职业定位分析 → 项目读取 → LLM生成简历 | ✅ 完整 |
| Step 3 | 关键词提取 → 多平台并行搜索 → 匹配评分 → 排序 | ✅ 完整 |
| Step 4 | JD分析 → 定制简历生成 → BOSS发招呼/一键复制 → 飞书文档 | ✅ 完整 |
| Step 5 | 意图分类（14类） → 回复生成 → 自动/确认发送 | ✅ 完整 |
| Step 6 | 识别请求类型 → 发送定制简历/联系方式 → 确认 | ✅ 完整 |
| Step 7 | JD+简历分析 → 分类面题生成 → 回答方向 → 模拟面试 | ✅ 完整 |
| Step 8 | 复盘报告生成 → 索引更新 → 偏好更新 | ✅ 完整 |

### 2.3 各 Step 异常处理 → 覆盖度评估

| Step | 异常处理条目数 | 覆盖场景 | 评估 |
|------|--------------|---------|------|
| Step 1 | 3条（平台失败/内容不足/扫描超时） | 较全 | ✅ |
| Step 2 | 2条（量化成果缺失/职业定位模糊） | 基本覆盖 | ✅ |
| Step 3 | 3条（平台失败/无匹配职位/搜索超时） | 基本覆盖 | ✅ |
| Step 4 | 3条（BOSS限制/PDF失败/JD无法访问） | 基本覆盖 | ✅ |
| Step 5 | 2条（意图不明/薪资未配置） | **偏少** | ⚠️ |
| Step 6 | 2条（简历丢失/联系方式不完整） | 基本覆盖 | ✅ |
| Step 7 | 2条（JD不完整/面题过少） | 基本覆盖 | ✅ |
| Step 8 | 2条（数据不完整/无offer） | 基本覆盖 | ✅ |

**注意：** Step 5 异常处理偏少，建议补充以下场景：
- HR 连续发送多条消息时的处理（上下文关联）
- 用户未开启「自动回复」但期望自动发送的情况
- LLM 生成回复质量不满足阈值时的降级

---

## 三、优先级合理性审查

### 3.1 MVP 范围（Phase 1）

| 功能 | 标注优先级 | 评估 |
|------|-----------|------|
| 数字足迹聚合 | P0 | ✅ 合理，核心入口 |
| 基础简历生成 | P0 | ✅ 合理，核心入口 |
| 多平台岗位搜索 | P0 | ✅ 合理，核心功能 |
| JD 匹配度评分 | P0 | ✅ 合理，核心功能 |
| 定制简历 HTML→PDF | P0 | ✅ 合理，核心功能 |
| BOSS直聘发招呼 | P1 | ✅ 已降级，合理 |
| 飞书Bitable职位追踪 | P0 | ✅ 合理 |
| HR沟通辅助 | P1 | ⚠️ **可能低估**（见下方）|
| 每日定时巡检 | P1 | ✅ 合理 |

**疑问：** Step 5（HR沟通辅助）标注为 P1，但实际用户体验中「收到HR回复后能否快速、高质量地沟通」是影响求职成败的关键节点。如果 MVP 的核心闭环是「投递 → 入职」，HR沟通是高频且关键的环节。

**建议：** 考虑将 HR 沟通辅助列入 MVP 核心闭环，或至少在 Phase 1 末期纳入 demo 验证。

### 3.2 Phase 2/3 优先级

| 功能 | 标注优先级 | 评估 |
|------|-----------|------|
| Step 6 发送联系方式 | P1 | ✅ 合理 |
| Step 7 个性化面题库 | P1 | ✅ 合理 |
| Step 7 模拟面试 | P1 | ✅ 合理 |
| Step 8 求职复盘 | P2 | ✅ 合理 |
| 薪资谈判辅助 | P2 | ✅ 合理 |
| 简历多版本管理 | P2 | ✅ 合理 |
| 团队协作版 | P3 | ✅ 合理，长期愿景 |
| 模拟面试语音模式 | P3 | ✅ 合理 |

**评估：** 优先级整体合理，Phase 2/3 功能范围适中。

---

## 四、用户体验顺畅度审查

### 4.1 用户旅程连贯性

```
「开始求职」(Step 1)
    ↓
数字足迹确认
    ↓
「生成简历」(Step 2)
    ↓
简历预览 + 编辑 + 确认
    ↓
「搜索合适工作」(Step 3)
    ↓
职位列表筛选 + 确认投递列表
    ↓
「定制简历投递」(Step 4)
    ↓
定制简历预览 + 确认 → BOSS发招呼/一键复制
    ↓
等待 HR 回复...
    ↓
「帮我回复」+ HR消息 (Step 5)
    ↓
回复建议 → 自动发送/确认后发送
    ↓
HR要求联系方式 (Step 6)
    ↓
确认 → 发送
    ↓
「准备面试」(Step 7)
    ↓
面题库 + 模拟练习
    ↓
offer / 求职告一段落 (Step 8)
    ↓
复盘报告
```

**评估：** 8步流程逻辑清晰，节点间衔接自然，用户旅程顺畅。

### 4.2 确认点设置合理性

| 确认点 | 位置 | 是否必要 | 评估 |
|--------|------|---------|------|
| Step 1 数字足迹确认 | Step 1→Step 2 | ✅ | 必要，防止遗漏重要内容 |
| Step 2 简历预览确认 | Step 2→Step 3 | ✅ | 必要，简历是后续所有步骤的基础 |
| Step 3 职位选择确认 | Step 3→Step 4 | ✅ | 必要，用户自主选择投递目标 |
| Step 4 定制简历确认 | Step 4→投递 | ✅ | 必要，确保定制内容正确 |
| Step 5 高敏感消息确认 | 约面/薪资/Offer | ✅ | 必要，这些决策影响重大 |
| Step 6 联系方式确认 | 发送前 | ✅ | 必要，必须用户确认 |
| Step 7 面题库确认 | 生成后 | ⚠️ 可选 | 用户可选择跳过直接练习 |
| Step 8 复盘报告确认 | 数据沉淀前 | ✅ | 必要，确保复盘内容准确 |

**评估：** 确认点设置整体合理，Step 7 的面题库确认可考虑改为可选（用户可跳过确认直接进入练习）。

### 4.3 异常流程用户体验

**发现：** 文档中异常处理偏重「系统侧异常」（平台失败/超时/内容不足），对「用户侧异常」覆盖不足：

| 缺失场景 | 影响 |
|---------|------|
| 用户中途放弃流程 | 无机制保存进度，用户下次需重新开始 |
| 用户多次修改简历后重新投递 | 无版本对比机制 |
| 用户投递后被拒但仍想继续投递同公司 | 无「重新激活职位」机制 |
| 用户主动要求回退到上一步 | 无回退机制描述 |

**建议：** 在异常处理章节补充「用户侧异常」场景，或在 UX 设计中明确「允许用户回退」的行为约定。

---

## 五、其他遗留问题

### 5.1 数据路径不一致（方案文档 vs PRD）

**问题：** 如 3.3 节所述，方案文档仍使用 `~/ProjectTrace/projects/`，与 PRD 规定的 `~/.jobtracer/footprint/projects/` 不一致。

**影响范围：** 代码实现时若参考方案文档，会导致路径错误。

**建议：** 以 PRD 为准，方案文档同步更新。

---

### 5.2 ProjectTrace 聚类引擎能力边界未明确

**问题：** Step 1 依赖 ProjectTrace 的「Clustering Engine」进行项目聚类，但 PRD 未说明：
- 聚类不准确时的兜底策略
- 多技能用户的项目归类原则（如：同时擅长前端和后端的项目如何归类）
- 「冷启动」场景（用户数字足迹极少时）如何处理

**建议：** 在 Step 1 异常处理中补充：
- 聚类置信度低于阈值时的处理（降级为手动指定项目）
- 至少保留一个兜底聚类方案（按时间线聚类，而非按项目聚类）

---

### 5.3 Step 3 多平台并行搜索的结果聚合策略

**问题：** Step 3 提到「并行搜索多平台」，但未说明：
- 当某个平台超时/失败时，已获取结果的返回时机
- 多平台结果去重规则（同一公司同一职位在不同平台的去重）
- 搜索超时总时间上限

**建议：** 补充：
- 总搜索超时上限（如：30秒内返回已获取结果）
- JD 去重规则（基于 company + title + location 匹配）

---

### 5.4 Step 5 消息队列与上下文关联

**问题：** Step 5 是基于单条 HR 消息处理的，但实际场景中 HR 可能连续发送多条消息，存在上下文关联：
- HR 先问「最快什么时候能入职？」，用户回答后，HR 再问「薪资期望是多少？」
- 当前 Step 5 的设计是逐条处理，无法关联上下文

**建议：** Step 5 增加「多轮对话上下文」设计：
- 记录当前对话窗口的 HR 意图历史
- 生成回复时参考同一对话窗口内的历史消息

---

### 5.5 Step 7 模拟面试模式的交互流程

**问题：** Step 7 提到「模拟面试模式：用户说『练习面试』，JobTracer 随机出题，用户回答后给出反馈」，但：
- 用户通过什么渠道回答？（飞书消息/语音/文字）
- 反馈机制是什么？（AI 即时反馈/收集后统一反馈）
- 如何判断用户回答质量？

**建议：** 补充模拟面试的具体交互设计：
- 建议使用文字+语音双通道（飞书支持）
- AI 即时反馈：每回答一题立即给出点评
- 整体评估：模拟面试结束后给出综合评分

---

### 5.6 数据新鲜度与同步机制

**问题：** PRD 未说明以下数据的刷新机制：
- `job-tracker.json` 的职位列表多久更新一次
- JD 缓存（`jd_cache/`）的过期时间
- 简历版本历史（`resume_versions/`）的保留策略

**建议：** 补充：
- 职位列表建议每天刷新一次（同岗位已下线需标记）
- JD 缓存建议保留 7 天后过期重新拉取
- 简历版本历史建议保留最近 5 个版本

---

### 5.7 移动端体验未覆盖

**问题：** PRD 在「兼容性」章节提到「飞书桌面版 + 移动版（iOS/Android）」，但文档中未说明：
- 哪些功能在移动端可用/不可用
- PDF 简历预览在移动端的体验
- 模拟面试的移动端交互方式

**建议：** 补充移动端适配说明，或标注「MVP 优先支持桌面端」。

---

### 5.8 成功指标缺失

**问题：** PRD 缺少明确的「产品成功指标」定义：
- 简历生成满意度（用户确认率）
- 匹配度评分准确率
- HR 沟通辅助使用率
- 面试准备满意度

**建议：** 补充 Phase 1 成功指标：
- 数字足迹扫描覆盖率 ≥ 80%（用户已有文档）
- 简历生成一次通过率 ≥ 70%（用户无需大幅修改）
- JD 匹配度 Top 10 中至少 3 个进入面试

---

## 六、总体评价

### 6.1 修复情况总结

| 上次问题 | 修复状态 |
|---------|---------|
| Step 5-8 描述深度不足 | ✅ 已修复 |
| BOSS直聘风险 | ✅ 已降级 |
| 数据路径不统一 | ⚠️ 部分未修复（方案文档偏差） |
| Step 5 确认逻辑 | ✅ 已修复 |

### 6.2 新发现问题汇总

| 问题 | 严重程度 | 建议 |
|------|---------|------|
| 方案文档路径与 PRD 不一致 | 高 | 以 PRD 为准，同步方案文档 |
| Step 5 异常处理偏少 | 中 | 补充多轮对话/上下文关联场景 |
| 聚类引擎能力边界未明确 | 中 | 补充兜底策略和冷启动方案 |
| 多平台搜索结果聚合策略 | 中 | 补充超时机制和去重规则 |
| 模拟面试交互流程模糊 | 中 | 补充具体交互设计 |
| 移动端体验未覆盖 | 低 | 标注 MVP 端侧重点 |
| 成功指标缺失 | 低 | 补充 Phase 1 成功指标 |
| 用户中途回退机制 | 低 | 补充回退行为约定 |

### 6.3 最终结论

**整体评价：** PRD v1.0.0 结构完整，Step 1-8 功能描述清晰，优先级合理，核心用户体验设计（确认点机制、降级策略、隐私保护）扎实。上次评审提出的 4 个主要问题中，3 个已完全修复，1 个部分修复（数据路径在 PRD 中已统一，但方案文档未同步）。

**建议：**
1. 以 PRD `~/.jobtracer/` 为准，方案文档同步更新路径
2. 建议将 Step 5（HR沟通辅助）列入 MVP Phase 1 末期验证
3. 补充上述中/低优先级的增强项

**是否通过：** ✅ **通过，有条件认可**（条件：方案文档路径需同步，Step 5 建议纳入 Phase 1 验证计划）

---

*本报告由分析师 Agent 生成*